export default function PrivacyPage() {
  return (
    <main className="app-shell">
      <h1 className="gold-text">Privacy Policy (Simulation)</h1>
      <div className="card" style={{ fontSize: 13, lineHeight: 1.7 }}>
        <p>On entry, we record your IP address, user agent string, and a
        timestamp as a compliance log. This record is append-only and is used
        solely to demonstrate legal-proof logging patterns.</p>
        <p>Replace this placeholder with a real privacy policy — reviewed for
        your jurisdiction's data protection law (e.g. India's DPDP Act) —
        before any public deployment.</p>
      </div>
    </main>
  );
}
